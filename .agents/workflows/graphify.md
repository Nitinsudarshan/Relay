---
name: graphify
description: Turn any folder of files into a navigable knowledge graph
---

# Workflow: graphify

Follow the graphify skill to run the full pipeline.

If no path argument is given, use `.` (current directory) — this covers the whole polyglot repo (Rust backend + both React frontends) in one graph, not one per surface.
